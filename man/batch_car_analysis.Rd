% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_car_analysis.R
\name{batch_car_analysis}
\alias{batch_car_analysis}
\title{Batch CAR Analysis for Multiple Samples}
\usage{
batch_car_analysis(
  sample_folder,
  db_output_folder,
  edge_output_folder = "edge_lists",
  top50_file = "Top50_formula.csv",
  car_min = 0.45,
  car_max = 1,
  small_molecules = c("C", "O", "CH2", "H2O", "CO", "O2", "CO2", "NH3", "NH"),
  export_summary = TRUE,
  export_edges = TRUE,
  summary_wide_file = "Top50_All_Samples_Summary.csv",
  summary_long_file = "Top50_All_Samples_Long.csv",
  verbose = TRUE
)
}
\arguments{
\item{sample_folder}{Character. Path to folder containing sample CSV files}

\item{db_output_folder}{Character. Path where SQLite databases will be saved}

\item{edge_output_folder}{Character. Path where edge list files will be saved}

\item{top50_file}{Character. Path to Top50_formula.csv file (default: "Top50_formula.csv")}

\item{car_min}{Numeric. Minimum CAR threshold (default: 0.45)}

\item{car_max}{Numeric. Maximum CAR threshold (default: 1.0)}

\item{small_molecules}{Character vector. Molecules to filter out}

\item{export_summary}{Logical. Whether to export summary tables (default: TRUE)}

\item{export_edges}{Logical. Whether to export edge lists (default: TRUE)}

\item{summary_wide_file}{Character. Output filename for wide summary table}

\item{summary_long_file}{Character. Output filename for long summary table}

\item{verbose}{Logical. Whether to print progress messages (default: TRUE)}
}
\value{
A list containing:
\itemize{
  \item{db_paths: }{Named list of database file paths}
  \item{edge_paths: }{Named list of edge file paths}
  \item{wide_table: }{Data frame with samples as columns}
  \item{long_table: }{Data frame with one row per sample-formula combination}
  \item{stats: }{Summary statistics}
}
}
\description{
Processes multiple CSV files to generate CAR matrices in SQLite databases,
then queries and summarizes Top50 molecular formula occurrences across all samples.
Also exports Source-Target edge lists for each sample.
}
\examples{
\dontrun{
# Basic usage
result <- batch_car_analysis(
  sample_folder = "path/to/samples",
  db_output_folder = "path/to/databases",
  edge_output_folder = "path/to/edges"
)

# Access results
print(result$wide_table)
print(result$stats)
}
}
